## 1. Fix Manifest

- [x] 1.1 Change `start_url: '/'` to `start_url: './'` in `client/vite.config.ts`

## 2. Deploy and Verify

- [x] 2.1 Commit, deploy via `caprover deploy`, then test "Add to Home Screen" from a list page and confirm the shortcut opens that list
